// import 'dotenv/config';
// const {ORIGIN_DEV, ORIGIN_PROD} = process.env;
// export const {TEST, PORT} = process.env;

// export const ORIGIN = process.env.NODE_ENV === 'production' ? ORIGIN_PROD : ORIGIN_DEV;
